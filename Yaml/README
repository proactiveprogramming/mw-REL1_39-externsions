YAML extension for MediaWiki
This database extension allows for key/value storage and retrieval
--------------------------------

YAML files should be writable

Add the following to LocalSettings.php
require_once( "$IP/extensions/YAML/YAML.php" );

Read Example
----
{{#yaml:db|0000}}

Write Example
----
{{#yaml:db|0000|Unknown}}
